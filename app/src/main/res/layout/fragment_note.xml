<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#dbdbd9"
    tools:context="com.lulu.todayinhistory.mvp.view.fragment.NoteFragment">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:background="#ebe7e7"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/dataTime"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="6"
            android:gravity="center"
            android:text=""
            android:textColor="#000000"
            android:textSize="18sp" />

        <ImageButton
            android:visibility="gone"
            android:id="@+id/btn_menu"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="center_vertical"
            android:layout_marginRight="20dp"
            android:background="@mipmap/add"
            android:textColor="#000000"
            android:textSize="18sp"/>
    </LinearLayout>

    <com.lulu.todayinhistory.customview.MyTextEditext
        android:id="@+id/mytxt"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="60dp"
        android:background="@null"
        android:gravity="top|left"
        android:inputType="textMultiLine"
        android:lineSpacingExtra="10dp"
        android:lineSpacingMultiplier="1.5"
        android:textSize="20sp" />
    <!--右上-->
    <com.lulu.todayinhistory.customview.SatelliteMenu
        android:id="@+id/mSatelliteMenuRightTop"
        android:layout_width="match_parent"
        android:layout_height="300dp"
        app:menu_image_width="45dp"
        app:menu_item_image_width="35dp"
        app:menu_postion="right_top"
        app:radius="130dp" />
</FrameLayout>
